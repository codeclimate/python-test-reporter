machine:
  services:
    - docker

dependencies:
  override:
    - pip install codeclimate-test-reporter
    - make image

test:
  override:
    - make citest
    - codeclimate-test-reporter --file /tmp/coverage.txt
