machine:
  python:
    version: 3.5.1

dependencies:
  override:
    - pip install -r requirements.txt
    - python setup.py install

test:
  override:
    - python setup.py test
    - codeclimate-test-reporter
